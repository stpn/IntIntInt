<h1>Parsed Videos</h1>

<table>
  <tr>
    <th>Youtube ID</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @videos.each do |video| %>
  <tr>
    <td><%= video.content %></td>
    <td><%= link_to 'Show Comments', video %></td>
 <!--   <td><%= link_to 'Edit', edit_video_path(video) %></td> -->
    <td><%= link_to 'Destroy', video, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>


<br />

<%= link_to 'New Query', new_video_path %>
